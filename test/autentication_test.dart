import 'package:flutter_test/flutter_test.dart';

void main() {
  test('initial state is LoginInitial', () {
    expect(1, 1);
    expect(1, 1);
  });

  group('login', () {
    test('username and password are corect', () async {
      expect(1, 1);
    });

    test('username and password are not corect', () async {
      expect(1, 1);
    });
  });
}
